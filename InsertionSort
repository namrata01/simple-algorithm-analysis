def insertionSort(mylist):
    for i in range(1,len(mylist)):
        key = mylist[i]
        j = i-1
        while (mylist[j]>key and j>=0):
            mylist[j+1]=mylist[j]
            j=j-1
        mylist[j+1]=key
    print mylist

mylist = [3,2,1,5,4,0]
insertionSort(mylist)
